<div class="modal popover" data-modal>
  <div class="modal__wash"></div>
  <div class="modal__inner" data-scroll-lock-ignore>
    <div class="modal__header">
      <button
      type="button"
      data-modal-close
      class="modal__close-icon"
      aria-label="{{ 'general.accessibility.close_modal' | t }}"
      >
        {% render 'icon-button' with icon, icon: 'close-stroke' %}
      </button>
    </div>
    <div class="modal__content rte rte--page"></div>
  </div>
</div>
{% comment %}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% render 'icon-button' with icon: 'close-stroke' %}"
  to: "{% render 'icon-button' with icon, icon: 'close-stroke' %}"
  position: 328..386
{% endcomment %}